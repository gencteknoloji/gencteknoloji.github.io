import fs from 'fs'; const f = 'src/app/dashboard/DashboardHome.tsx'; let c = fs.readFileSync(f, 'utf8'); const idx = c.indexOf('\" use "client\;', 5); if(idx  { c = c.substring(idx); } c = c.replace('import { formatDateISO } from \'@/lib/utils/date\';\r\n  ConfirmModalState,', 'import { formatDateISO } from \'@/lib/utils/date\';\r\nimport type { AuthUser, Cari, CariForm, CariTransaction, ChartPoint, ConfirmModalState,'); fs.writeFileSync(f, c);  
